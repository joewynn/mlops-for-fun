# app/source/utils.py

from typing import List
class SeedClass: 
    def __init__(self):
        self.seed = 42

    def set_seed(self):
        return self.seed
    
    def set_learning_rates(self, remote: str, inline: List = STOPWORDS) -> str:
        """Set learning rates

        Args:
            remote (str): the input remote value
            inline (value, optional): come one now. Defaults to STOPWORDS.

        Returns:
            str: new result value
        """
        return strWord = f'What is that {remote} Value with {List}'

    def new_file(a: int, c=[1,2]): 
        """_summary_

        Args:
            a (_type_): _description_
            c (_type_, optional): _description_. Defaults to [1,2].

        Raises:
            AssertionError: _description_

        Returns:
            _type_: _description_
        """        
        if a > 10: 
            raise AssertionError("a is more than ten")
        
        return c